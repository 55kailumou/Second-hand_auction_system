<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.MessageMapper">

    <sql id="messageColumns">
        id, user_id, type, title, content, related_id, is_read, create_time
    </sql>

    <!-- 1. 按 ID 查询 -->
    <select id="findById" parameterType="int" resultType="Message">
        SELECT <include refid="messageColumns"/>
        FROM message
        WHERE id = #{id}
    </select>

    <!-- 2. 插入消息 -->
    <insert id="insert" parameterType="Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (user_id, type, title, content, related_id)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{relatedId})
    </insert>

    <!-- 3. 条件查询（按用户 + 已读/未读） -->
    <select id="findByCondition" parameterType="map" resultType="Message">
        SELECT <include refid="messageColumns"/>
        FROM message
        <where>
            user_id = #{userId}
            <if test="isRead != null">
                AND is_read = #{isRead}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 4. 计数（同条件） -->
    <select id="countByCondition" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM message
        <where>
            user_id = #{userId}
            <if test="isRead != null">
                AND is_read = #{isRead}
            </if>
        </where>
    </select>

    <!-- 5. 未读数 -->
    <select id="countUnreadByUserId" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM message WHERE user_id = #{userId} AND is_read = 0
    </select>

    <!-- 6. 标记单条已读 -->
    <update id="markRead">
        UPDATE message
        SET is_read = 1
        WHERE id = #{id} AND user_id = #{userId} AND is_read = 0
    </update>

    <!-- 7. 全部已读 -->
    <update id="markAllRead">
        UPDATE message
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>

    <!-- 8. 删除单条 -->
    <delete id="deleteById">
        DELETE FROM message WHERE id = #{id} AND user_id = #{userId}
    </delete>

</mapper>
