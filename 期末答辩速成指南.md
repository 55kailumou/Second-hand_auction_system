# 🎯 二手物品拍卖系统 — 期末答辩速成指南

> 这份文档帮你用*最小学习成本*应付老师的代码提问。
> 你不需要理解每一行代码，只需要知道**关键知识点** + **老师可能问什么 + 怎么答**。

---

## 📦 一、这个项目是做什么的？(一句话版本)

> **一个二手物品拍卖网站**，用户可以在上面发布商品让别人竞拍（像闲鱼 + 拍卖行的结合体），包括用户注册登录、发布商品、出价竞拍、下单支付(模拟)、信用评价、投诉处理，以及管理员后台管理全部流程。

---

## 🧱 二、技术栈速记（老师必问）

| 层 | 技术 | 一句话说明 |
|---|---|---|
| **前端页面** | JSP + Vue 3 | JSP 是服务端模板（Java里写HTML），Vue 3 做前端动态交互 |
| **后端** | Servlet (Java) | 处理 HTTP 请求的 Java 类，相当于后端"控制器" |
| **数据层** | MyBatis 3 | 把 Java 方法调用转成 SQL 语句操作数据库 |
| **数据库** | MySQL 8 | 存用户、商品、订单等数据 |
| **服务器** | Tomcat 9 | 运行 Java Web 应用的容器 |
| **构建工具** | Maven | 自动化下载 jar 包 + 编译打包 |

> **老师如果问：「你们项目用的是什么架构？」**
> 答：**MVC 架构** — Model（实体类+MyBatis 数据层）、View（JSP 页面）、Controller（Servlet 控制器）。

---

## 🏗 三、项目目录结构（老师常问"你的代码放在哪"）

```
src/main/
├── java/org/example/
│   ├── entity/        →  数据模型（13个类，对应数据库表）
│   ├── mapper/        →  MyBatis接口（15个，定义数据库操作方法）
│   ├── servlet/       →  控制器（19个，处理请求，相当于"后端API"）
│   ├── service/       →  业务逻辑（2个，出价结算、消息通知）
│   ├── filter/        →  过滤器（3个，登录检查、编码设置）
│   └── util/          →  工具类（4个，MyBatis连接、密码加密等）
│
├── resources/
│   ├── db.properties         →  数据库配置（改这里连你的数据库）
│   ├── mybatis-config.xml    →  MyBatis全局配置
│   └── org/example/mapper/   →  SQL映射文件（15个，写SQL的地方）
│
└── webapp/
    ├── index.jsp           →  首页
    ├── static/             →  CSS/JS静态资源
    └── WEB-INF/
        ├── web.xml         →  Web部署配置
        └── jsp/            →  所有JSP页面
```

---

## 🔄 四、一次请求的完整流程（老师必考！）

> 这是最核心的知识点。老师一定会问：「**用户在浏览器点了一个按钮，后端是怎么处理的？**」

### 标准流程（4步）

```
浏览器请求  →  ① Filter过滤器  →  ② Servlet控制器  →  ③ JSP页面
                                              ↓
                                          MyBatis
                                              ↓
                                           MySQL数据库
```

### 举个具体例子：用户登录

```
① 用户在 login.jsp 输入账号密码，点"登录"
② 浏览器 POST 请求到 /user?action=login
③ EncodingFilter 先执行 → 设置 UTF-8 编码
④ AuthFilter 检查 → 登录页面是公开的，放行
⑤ UserServlet 收到请求
   → 调用 UserMapper.login(username, password)
   → MyBatis 执行 SQL: SELECT * FROM user WHERE username=? AND password=?
   → 查到了 → 把用户信息存到 session（会话）
   → 重定向到首页
```

### 关键概念速记

| 概念 | 一句话解释 | 老师问你怎么答 |
|---|---|---|
| **Servlet** | 处理请求的 Java 类，类似后端的"路由处理器" | "Servlet 接收 HTTP 请求，调用数据库，返回结果或跳转页面" |
| **JSP** | 里面可以写 HTML + Java 代码 | "JSP 用来展示数据，里面用 EL 表达式 `${}` 和 JSTL 标签显示后端传来的数据" |
| **Session** | 服务端保存的用户会话信息 | "用户登录后把用户信息存在 session 里，后续请求通过 session 判断是否已登录" |
| **Filter** | 请求到达 Servlet 之前的"关卡" | "Filter 用来做拦截，比如没登录就跳转到登录页，或者设置请求的编码格式" |
| **MyBatis** | 把 Java 方法和 SQL 绑定的框架 | "MyBatis 让我在 XML 里写 SQL，在 Java 里直接调用方法就能操作数据库" |
| **EL表达式** | `${user.username}` 在 JSP 里输出数据 | "EL 表达式用来在 JSP 页面中获取后端传过来的数据" |

---

## 🗄 五、数据库表结构（老师可能问"数据库怎么设计的"）

核心表（共13张）：

| 表名 | 作用 | 关键字段 |
|---|---|---|
| **user** | 用户表 | id, username, password(SHA-256), phone, credit_score(信用分), balance(余额) |
| **admin** | 管理员表 | id, admin_account, admin_password, role(角色: super/admin/normal) |
| **auction_item** | 拍卖品表 | id, seller_id, category_id, title, start_price, current_price, start_time, end_time, status(0草稿→5已结束) |
| **bid_record** | 出价记录表 | id, item_id, bidder_id, bid_amount, is_winning(是否中标) |
| **order_info** | 订单表 | id, order_no(UUID), item_id, buyer_id, seller_id, final_price, status(0待付款→6已完成) |
| **category** | 分类表 | id, category_name, parent_id(0=一级分类), sort_order |
| **notice** | 公告表 | id, title, content, is_top(是否置顶), status |
| **complaint** | 投诉表 | id, order_id, complainant_id(投诉人), respondent_id(被投诉人), status |
| **credit_record** | 信用评价表 | id, order_id, evaluator_id, target_id, score(1-5) |
| **message** | 消息表 | id, user_id, type(1-7), title, content, is_read |
| **address** | 地址表 | id, user_id, receiver_name, phone, province/city/district, is_default |
| **watch_list** | 收藏表 | id, user_id, item_id |
| **item_image** | 商品图片表 | id, item_id, image_url, sort_order |

---

## 🔑 六、必须理解的 5 个核心功能代码

### 1. 用户登录（最常被问！）

**JSP 页面** (`login.jsp`) — 用户填表单的地方：
```jsp
<form @submit.prevent="handleLogin">
  <input v-model="loginForm.account" placeholder="手机号/邮箱">
  <input v-model="loginForm.password" type="password" placeholder="密码">
  <button type="submit">登录</button>
</form>
```

**Servlet** (`UserServlet.java`) — 处理登录请求：
```java
// 关键代码：收到请求后，判断 action 参数
String action = req.getParameter("action");
if ("login".equals(action)) {
    String account = req.getParameter("account");
    String password = PasswordUtil.encrypt(req.getParameter("password"));
    
    // 调用 MyBatis 查询数据库
    User user = userMapper.login(account, password);
    
    if (user != null) {                    // 查到用户 → 登录成功
        req.getSession().setAttribute("currentUser", user);  // 存入 session
        resp.sendRedirect(returnUrl);
    } else {                                // 没查到 → 登录失败
        req.setAttribute("errorMsg", "账号或密码错误");
        req.getRequestDispatcher("/WEB-INF/jsp/user/login.jsp").forward(req, resp);
    }
}
```

> 💡 **老师问：「密码怎么存的？」** 答：SHA-256 哈希加密，不存明文。`PasswordUtil.java` 里用 `MessageDigest` 实现。

### 2. 发布拍卖品（体现业务逻辑）

**Servlet** (`ItemServlet.java`)：
```java
if ("publish".equals(action)) {
    AuctionItem item = new AuctionItem();
    item.setSellerId(currentUser.getId());    // 当前用户是卖家
    item.setCategoryId(Integer.parseInt(req.getParameter("categoryId")));
    item.setTitle(req.getParameter("title"));
    item.setStartPrice(new BigDecimal(req.getParameter("startPrice")));
    item.setCurrentPrice(item.getStartPrice()); // 当前价 = 起拍价
    item.setStartTime(new Date());              // 发布时间 = 现在
    // ... 省略其他字段
    auctionItemMapper.insert(item);             // 保存到数据库
    resp.sendRedirect("item?action=my-items");  // 跳转到"我的拍品"
}
```

### 3. 出价竞拍（含并发控制，加分点！）

**核心代码** (`BidServlet.java`)：
```java
// 1. 先查最新价格
AuctionItem item = auctionItemMapper.findById(itemId);
BigDecimal oldPrice = item.getCurrentPrice();

// 2. 用"乐观锁"更新：CAS（Compare And Swap）思想
int rows = auctionItemMapper.updatePriceOptimistic(
    itemId, oldPrice, oldPrice.add(bidIncrement)
);
// 如果 rows == 0，说明价格已经被别人改了，出价失败！

if (rows > 0) {
    // 3. 记录出价记录
    BidRecord record = new BidRecord(itemId, userId, bidAmount);
    bidRecordMapper.insert(record);
}
```

> 💡 **老师问：「多人同时出价怎么防止冲突？」**
> 答：用了**乐观锁**。更新时判断当前价格是否还是我查到的那个价格，`UPDATE ... WHERE current_price = #{oldPrice}`，如果被别人改了，这条 SQL 影响 0 行，就知道出价失败了。

### 4. 过滤器（拦截器，老师爱问）

**登录校验** (`AuthFilter.java`)：
```java
public void doFilter(request, response, chain) {
    HttpSession session = request.getSession(false);
    User user = (session != null) ? session.getAttribute("currentUser") : null;
    
    if (user != null) {
        chain.doFilter(request, response);  // 已登录 → 放行
    } else {
        // 没登录 → 跳转到登录页
        response.sendRedirect(request.getContextPath() + "/user?action=login");
    }
}
```

### 5. MyBatis 配置（数据层基础）

**mybatis-config.xml**：
```xml
<!-- 引用数据库配置 -->
<properties resource="db.properties"/>

<!-- 数据库连接 -->
<dataSource type="POOLED">
    <property name="driver" value="${jdbc.driver}"/>
    <property name="url" value="${jdbc.url}"/>
    <property name="username" value="${jdbc.username}"/>
    <property name="password" value="${jdbc.password}"/>
</dataSource>
```

**Mapper XML**（SQL 写在 XML 里）：
```xml
<!-- UserMapper.xml -->
<select id="login" resultType="User">
    SELECT * FROM user 
    WHERE (username = #{account} OR phone = #{account}) 
    AND password = #{password}
</select>

<insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO user(username, password, phone, ...) 
    VALUES(#{username}, #{password}, #{phone}, ...)
</insert>
```

---

## 🚦 七、订单状态机（体现思路完整性）

```
0-待付款  →  1-待发货  →  2-已发货  →  3-已收货  →  6-已完成
    ↓            ↓    
  4-已取消    5-退款中 → 退款处理(管理员)
```

老师问"订单状态怎么管理"——就背这个状态机。

---

## 👨‍💼 八、管理员功能速览

| 功能 | 说明 |
|---|---|
| 管理员登录 | `/admin/login`，默认账号 admin / 123456 |
| 用户管理 | 查看、封禁/解封、调整信用分、重置密码 |
| 商品管理 | 审核、下架、修改分类 |
| 分类管理 | 增删改分类（两级分类体系） |
| 公告管理 | 发布/撤回/置顶公告 |
| 投诉处理 | 审核投诉（同意/驳回） |
| 退款处理 | 审核退款申请 |
| 数据统计 | 用户数/商品数/订单数趋势图、分类分布、热门商品 |

---

## 🗣 九、老师可能问的高频问题（带答案！）

### Q1: 你们项目用的什么架构？
> **MVC架构**。Model（模型层）是 entity + MyBatis，封装数据和数据库操作；View（视图层）是 JSP 页面，负责展示；Controller（控制层）是 Servlet，负责接收请求、调用 Model、返回 View。

### Q2: JSP 和 Servlet 有什么区别？
> Servlet 是 Java 类，适合写逻辑处理（判断、计算、调数据库），但不方便输出 HTML；JSP 本质也是 Servlet，但更适合写 HTML 模板，里面可以用 `${}` 取数据。实际项目是 Servlet 处理完请求，把数据存到 request 或 session，然后 `forward` 到 JSP 来展示。

### Q3: 你们怎么连接数据库的？
> 用 MyBatis 框架。在 `mybatis-config.xml` 里配置了数据库连接（驱动、URL、用户名密码），然后在 `MyBatisUtil.java` 里用 `SqlSessionFactoryBuilder.build()` 读取这个配置文件，创建 `SqlSessionFactory`（单例模式），每次操作数据库通过 `factory.openSession()` 获取 `SqlSession`。

### Q4: 数据在页面和后台之间怎么传递的？
> 三种方式：1) **request.setAttribute()** → JSP 用 `${key}` 取；2) **session.setAttribute()** → 跨页面共享（如登录用户信息）；3) **AJAX**（用 Axios 库）→ 前端异步请求，后端返回 JSON，Vue 渲染。

### Q5: 用户登录怎么保持状态的？
> 用 **Session**。用户登录成功后，`req.getSession().setAttribute("currentUser", user)` 把用户信息存到服务端会话。后续每个请求，`AuthFilter` 从 session 里取 `currentUser`，取到了说明已登录，没取到就跳登录页。

### Q6: 你们哪里用了 Vue.js？
> 前端用了 Vue 3（通过 CDN 引入）做动态交互。比如登录页面的表单校验、商品列表的排序筛选分页、详情页的倒计时、发布页的草稿自动保存。这些如果用纯 JSP 写会很麻烦，Vue 让前端像现代应用一样响应式。

### Q7: SQL 写在 Java 代码里还是哪里？
> 写在 MyBatis 的 XML 映射文件里（`src/main/resources/org/example/mapper/`），不在 Java 代码里写 SQL。Java 代码（Mapper 接口）只声明方法名和参数，XML 里写具体 SQL。这样做的好处是：SQL 和 Java 代码分离，修改 SQL 不用重新编译 Java。

### Q8: 你们项目有什么亮点/难点？
> **亮点1：乐观锁防并发出价**。多人同时对同一商品出价，用 `UPDATE ... WHERE current_price = #{旧价格}` 保证只有一个成功。
> **亮点2：独立事务消息服务**。出价成功后发送通知消息，即使用户消息表写入失败也不影响出价主流程。
> **亮点3：Vue 3 前端增强**。在 JSP 旧技术上引入 Vue 做动态交互，兼顾了作业要求和前端体验。
> **亮点4：XSS 防护**。用 EscapeUtil 对用户输入的内容做 HTML/JS 转义，防止跨站脚本攻击。

### Q9: Filter 是什么？你们用了几个？
> Filter 是 Servlet 规范中的"过滤器"，在请求到达 Servlet 之前/之后执行。我们用了三个：
> 1. **EncodingFilter** — 设置请求和响应的 UTF-8 编码
> 2. **AuthFilter** — 检查用户是否登录，未登录跳转
> 3. **AdminAuthFilter** — 检查管理员是否登录，未登录跳转

### Q10: 怎么实现的上传图片？
> 这个项目没有做真正的图片上传。商品图片存在 MySQL 的 `image_url` 字段里（字符串类型），用的是外部图片链接。`ItemImage` 表也只存了 URL。

---

## 🎯 十、快捷键：快速定位代码（老师指哪你打哪）

| 老师问 | 看哪个文件 | 关键行/方法 |
|---|---|---|
| "登录怎么实现的？" | [UserServlet.java](src/main/java/org/example/servlet/UserServlet.java) | `if ("login".equals(action))` |
| "怎么验证登录的？" | [AuthFilter.java](src/main/java/org/example/filter/AuthFilter.java) | `doFilter()` 方法 |
| "数据库怎么连接的？" | [MyBatisUtil.java](src/main/java/org/example/util/MyBatisUtil.java) | `static { ... }` 静态块 |
| "SQL 写在哪？" | [UserMapper.xml](src/main/resources/org/example/mapper/UserMapper.xml) | `<select id="login">` |
| "出价逻辑在哪？" | [BidServlet.java](src/main/java/org/example/servlet/BidServlet.java) | `if ("place".equals(action))` |
| "发布商品在哪？" | [ItemServlet.java](src/main/java/org/example/servlet/ItemServlet.java) | `if ("publish".equals(action))` |
| "密码怎么加密的？" | [PasswordUtil.java](src/main/java/org/example/util/PasswordUtil.java) | `encrypt()` 方法 |
| "订单状态在哪改？" | [OrderMapper.xml](src/main/resources/org/example/mapper/OrderMapper.xml) | `updateStatus` 相关 |
| "首页在哪？" | [index.jsp](src/main/webapp/index.jsp) | 整个文件 |
| "管理员功能在哪？" | [AdminIndexServlet.java](src/main/java/org/example/servlet/AdminIndexServlet.java) | `doGet()` |
| "拍卖结束谁处理的？" | [AuctionEndService.java](src/main/java/org/example/service/AuctionEndService.java) | `settleAuction()` 方法 |

---

## 📋 十一、10 分钟速记清单（进考场前看这个）

```
□ 项目是 "二手物品拍卖系统"，MVC架构
□ 技术栈：JSP + Servlet + MyBatis + MySQL + Tomcat + Maven
□ 流程：请求 → Filter → Servlet → (调MyBatis→数据库) → JSP
□ 登录：UserServlet.login → userMapper.login() → 存 session
□ Session：用户登录状态的"身份证"
□ 出价防冲突：乐观锁（判断当前价格是否还是我查到的那个）
□ SQL：写在 XML 映射文件里，不在 Java 里
□ Filter：三个（编码、用户登录、管理员登录）
□ 密码：SHA-256 哈希（不可逆加密）
□ JSP用 ${} 取值，Vue 做前端交互
□ 订单状态：0→1→2→3→6 / 4取消 / 5退款中
□ 管理员：9个功能模块
□ 亮点：乐观锁 / 独立事务消息 / Vue增强 / XSS防护
```

---

## 📄 十二、代码阅读技巧（老师让你"解释这段代码"时）

### 看 Servlet 的套路：
```
① 找 @WebServlet("/xxx") → 看这个 Servlet 的 URL 是什么
② 找 doGet() / doPost() → 看请求来了怎么处理
③ 找 String action = req.getParameter("action") → 看有哪些操作
④ 找 session.getAttribute("currentUser") → 看是不是需要登录
⑤ 找 xxxMapper.xxx() → 看调了什么数据库操作
⑥ 找 forward/redirect → 看完了跳转到哪个页面
```

### 看 Mapper XML 的套路：
```
① 看 namespace → 对应哪个 Mapper 接口
② 看 id → 对应接口的哪个方法
③ 看 parameterType → 参数类型
④ 看 resultType / resultMap → 返回什么类型
⑤ 看 SQL 语句本身 → 其实就是你学过的标准 SQL
```

---

> **最后的建议：**
> - 老师大概率不会让你通读所有代码，而是**指着一两个关键文件**问你"这是干什么的"
> - 重点记住：**UserServlet.java**（登录注册）、**ItemServlet.java**（商品核心）、**AuthFilter.java**（拦截器）、**MyBatisUtil.java**（数据库连接）
> - 如果被问到不会的，诚实说"这块是参考了网上的实现，我的主要工作在 XXX 功能上"
